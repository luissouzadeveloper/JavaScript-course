let distancia = 185.72

let km = distancia / 1000
let hm = distancia / 100
let dam = distancia / 10
let dm = distancia * 10
let cm = distancia * 100
let mm = distancia * 1000

console.log(`${distancia} para km é ${km}`);
console.log(`${distancia} para hm é ${hm}`);
console.log(`${distancia} para dam é ${dam}`);
console.log(`${distancia} para dm é ${dm}`);
console.log(`${distancia} para cm é ${cm}`);
console.log(`${distancia} para mm é ${mm}`);